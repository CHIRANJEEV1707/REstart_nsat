"use client";

interface SmoothScrollProviderProps {
  children: React.ReactNode;
}

export function SmoothScrollProvider({ children }: SmoothScrollProviderProps) {
  // Simplified version without Lenis for now
  return <>{children}</>;
}
